# あとでやるリスト（TODO）

ユーザーが「あとでやる」と決めた項目。新セッション開始時はここも確認すること。
（恒久的な経緯・設計は `handoff.md` / `design.md` 参照）

---

## 🔜 端末間でデータを自動同期（クラウド同期）  ← ★リマインド対象（あとでやる）

- **状況**: 保留（ユーザー希望で後回し）。登録日 2026-06-29。
- **背景**: しおりのデータはブラウザ内ローカル(IndexedDB)のため、PCで作るとスマホには出ない。
  当面の手段として **JSON 書き出し/読み込み（実装済み）** で手動移行は可能。
- **やること（自動同期の本実装）**:
  - [ ] 方式決定：**Supabase（推奨）** か Firebase。どちらも無料枠で個人利用は十分。
  - [ ] **Googleログイン**（自分でパスワード管理は作らない。1タップ認証）。
  - [ ] trips を端末ローカル(IndexedDB)とクラウドで同期（オフライン編集→オンライン復帰でマージ）。
  - [ ] 競合解決（端末A/Bで別編集時）。最小は「updatedAt の新しい方」＋手動マージ余地。
  - [ ] 設計フェーズ4（クラウド同期・複数端末・公開）に相当。`design.md` §6 参照。
- **軽量な代替案（ログイン不要にしたい場合）**: 「同期コード（合言葉）」で端末を紐付け、
  サーバーレスKVに JSON を保管。実装は軽いが、コードを知る人にはデータが見える（プライバシー弱）。
- **メモ**: 開発環境は egress 制限が強く、外部API疎通テストは不可。実接続テストはユーザー側で行う想定。

---

## 次の候補（handoff §9 から・未着手）
- しおり: 号車・座席や宿泊もサマリーへ / 計画↔実績の切替 / 費用・割り勘。
- 地図×しおり連携の改善: 接続ギャップで遠回り/スキップになる区間、取込結果をパネルUI化、
  しおり側からの取込起動、塗った区間の leg 由来記録（RideRecord化）。
- Flutterネイティブ化（Android優先）。
- 地図: 実際の路線カラーに寄せる、県境の濃さ調整 等。
